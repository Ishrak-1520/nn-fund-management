<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================================ -->
    <!-- Top-Level Menu: Fund Management                               -->
    <!-- ============================================================ -->
    <menuitem
        id="menu_fund_management_root"
        name="Fund Management"
        sequence="45"
        web_icon="nn_fund_management,static/description/icon.png"
        groups="group_fund_user"/>

    <!-- ============================================================ -->
    <!-- Sub-Menu: Operations                                          -->
    <!-- ============================================================ -->
    <menuitem
        id="menu_fund_operations"
        name="Operations"
        parent="menu_fund_management_root"
        sequence="10"/>

    <menuitem
        id="menu_fund_accounts"
        name="Fund Accounts"
        parent="menu_fund_operations"
        action="action_fund_account"
        sequence="10"/>

    <menuitem
        id="menu_fund_incoming"
        name="Incoming Funds"
        parent="menu_fund_operations"
        action="action_fund_incoming"
        sequence="20"/>

    <menuitem
        id="menu_fund_allocations"
        name="Allocations"
        parent="menu_fund_operations"
        action="action_fund_allocation"
        sequence="30"/>

    <menuitem
        id="menu_fund_requisitions"
        name="Requisitions"
        parent="menu_fund_operations"
        action="action_fund_requisition"
        sequence="40"/>

    <menuitem
        id="menu_fund_bills"
        name="Fund Bills"
        parent="menu_fund_operations"
        action="action_fund_bills"
        sequence="50"/>

    <menuitem
        id="menu_fund_transfers"
        name="Transfers"
        parent="menu_fund_operations"
        action="action_fund_transfer"
        sequence="60"/>

    <!-- ============================================================ -->
    <!-- Sub-Menu: Master Data                                         -->
    <!-- ============================================================ -->
    <menuitem
        id="menu_fund_master_data"
        name="Master Data"
        parent="menu_fund_management_root"
        sequence="20"/>

    <menuitem
        id="menu_fund_projects"
        name="Projects"
        parent="menu_fund_master_data"
        action="action_fund_project"
        sequence="10"/>

    <menuitem
        id="menu_fund_expense_heads"
        name="Expense Heads"
        parent="menu_fund_master_data"
        action="action_fund_expense_head"
        sequence="20"/>

    <!-- ============================================================ -->
    <!-- Sub-Menu: Reporting                                           -->
    <!-- ============================================================ -->
    <menuitem
        id="menu_fund_reporting"
        name="Reporting"
        parent="menu_fund_management_root"
        sequence="30"/>

    <menuitem
        id="menu_approval_history"
        name="Approval History"
        parent="menu_fund_reporting"
        action="action_approval_history"
        sequence="10"/>

</odoo>
